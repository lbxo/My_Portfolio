# Lucas B Henderson Portfolio site

A collection of my works.

### Base Template

To manually set up the project, first download it with Git:

```bash
git clone https://github.com/bassjobsen/empty-bootstrap-project-gulp projectname
```